import React from 'react';
import ReactDOM from 'react-dom/client';
import ContextApp from './ThemeContext';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ContextApp />
  </React.StrictMode>
);